# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Config / validation ---
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0  # required for Pydantic's EmailStr

# --- Database ---
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
aiosqlite==0.20.0  # test database only

# --- Auth / security ---
pyjwt==2.10.1
bcrypt==4.2.1
cryptography==44.0.0

# --- Redis / caching ---
redis==5.2.1

# --- Trading / quant (wired in from Phase 4 onward) ---
ccxt==4.4.31
pandas==2.2.3
numpy==1.26.4

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.1
httpx==0.28.1
fakeredis==2.26.2  # in-memory Redis for tests — no real Redis required to run pytest

# --- Misc ---
python-dotenv==1.0.1
